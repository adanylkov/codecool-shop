@{
    ViewData["Title"] = "Home Page";
}

@model List<(Codecool.CodecoolShop.Models.Product Product, int Quanity)>

@foreach (var item in Model)
{
    @for (var i = 0; i < item.Quanity; i++)
    {
        @item.Product.Name;
    }
}
